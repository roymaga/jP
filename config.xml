<?xml version='1.0' encoding='utf-8' ?>
<widget xmlns     = "http://www.w3.org/ns/widgets"
        xmlns:gap = "http://phonegap.com/ns/1.0"
        id        = "com.JugaPlay"
		versionCode="25400"
        version   = "2.8.1">
    <name>JugaPlay</name>

    <description>
       Juga Play, la casa latinoamericana del fútbol fantasía. 
    </description>

    <author href="http://www.jugaplay.com" email="info@jugaplay.com">
        Jugaplay
    </author>
	<preference name="permissions"                value="none"/>

    <!-- Customize your app and platform with the preference element. -->
    <preference name='phonegap-version' value='cli-6.5.0' />          <!-- all: current version of PhoneGap -->
    <preference name="orientation"                value="portrait" />        <!-- all: default means both landscape and portrait are enabled -->
    <preference name="target-device"              value="universal" />      <!-- all: possible values handset, tablet, or universal -->
    <preference name="fullscreen"                 value="false" />           <!-- all: hides the status bar at the top of the screen -->
    <preference name="webviewbounce"              value="true" />           <!-- ios: control whether the screen 'bounces' when scrolled beyond the top -->
    <preference name="prerendered-icon"           value="true" />           <!-- ios: if icon is prerendered, iOS will not apply it's gloss to the app's icon on the user's home screen -->
    <preference name="stay-in-webview"            value="false" />          <!-- ios: external links should open in the default browser, 'true' would use the webview the app lives in -->
    <preference name="ios-statusbarstyle"         value="black-opaque" />   <!-- ios: black-translucent will appear black because the PhoneGap webview doesn't go beneath the status bar -->
    <preference name="detect-data-types"          value="true" />           <!-- ios: controls whether data types (such as phone no. and dates) are automatically turned into links by the system -->
    <preference name="exit-on-suspend"            value="false" />          <!-- ios: if set to true, app will terminate when home button is pressed -->
    <preference name="show-splash-screen-spinner" value="true" />           <!-- ios: if set to false, the spinner won't appear on the splash screen during app loading -->
    <preference name="auto-hide-splash-screen"    value="true" />           <!-- ios: if set to false, the splash screen must be hidden using a JavaScript API -->
    <preference name="disable-cursor"             value="false" />          <!-- blackberry: prevents a mouse-icon/cursor from being displayed on the app -->
    <preference name="android-minSdkVersion"      value="14" />              <!-- android: MIN SDK version supported on the target device. MAX version is blank by default. -->
    <preference name="android-installLocation"    value="auto" />           <!-- android: app install location. 'auto' will choose. 'internalOnly' is device memory. 'preferExternal' is SDCard. -->
	
	<!-- Record Audio -->
	<!-- Fin Record Audio -->
 <!-- Core plugins -->
    <gap:plugin name="cordova-plugin-device"  source="npm" />
	<plugin name="cordova-plugin-device-motion"   source="npm" />
	<plugin name="cordova-plugin-inappbrowser"   source="npm" spec="1.0.1" />

    <!-- A list of available plugins are available at https://build.phonegap.com/plugins -->
    <!--
        <gap:plugin name="com.phonegap.plugins.barcodescanner" /> -->
	<plugin name="cordova-plugin-x-socialsharing" source="npm" spec="5.0.7" />
	<gap:plugin name="cordova-plugin-backbutton" source="npm"/>
	<plugin name="cordova-plugin-network-information" source="npm"/>
	<plugin name="cordova-plugin-google-analytics" source="npm" />
	<gap:plugin name="cordova-plugin-whitelist" source="npm" />
	<!-- GeoLocation platform. -->
	
	<!-- Define app icon for each platform. -->	
	<icon src="imgapp/logo144.png" />
    <icon src="imgapp/logo36.png"   gap:platform="android"    gap:density="ldpi" />
    <icon src="imgapp/logo48.png"   gap:platform="android"    gap:density="mdpi" />
    <icon src="imgapp/logo72.png"   gap:platform="android"    gap:density="hdpi" />
    <icon src="imgapp/logo96.png"  gap:platform="android"    gap:density="xhdpi" />
	<icon src="imgapp/logo144.png" platform="android" qualifier="xxhdpi" />
	<icon src="imgapp/logo192" platform="android" qualifier="fr-xxhdpi" />
    <!-- Define app splash screen for each platform. -->
	<gap:splash src="imgapp/Default.png" gap:platform="android" gap:qualifier="ldpi" />
	<gap:splash src="imgapp/Default@2x.png" gap:platform="android" gap:qualifier="mdpi" />
	<gap:splash src="imgapp/Default@2x.png" gap:platform="android" gap:qualifier="hdpi" />
	<gap:splash src="imgapp/Default-568h@2x.png" gap:platform="android" gap:qualifier="xhdpi" />
	<gap:splash src="imgapp/Default-Portrait.png" gap:platform="android" gap:qualifier="fr-xhdpi" />
	<gap:splash src="imgapp/Default-Portrait@2x.png" gap:platform="android" gap:qualifier="port-xxhdpi" />
	<gap:splash src="imgapp/Default-LandscapeLeft@2x.png" gap:platform="android" gap:qualifier="land-xxhdpi" />
       

    <access origin="*"/>
	
</widget>
