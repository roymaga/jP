diff a/mobile/js/leagues/leagues.js b/mobile/js/leagues/leagues.js	(rejected hunks)
@@ -1,8 +0,24 @@
-// JavaScript Document
-window.onload=setTimeout(function(){showActualLeague();}, 1000);
+// Esta variable se agrega directamente en game.html
+// Es una forma prolija de bifurcar todo el loading.
+if(LOAD_TOP_TEN) {
+	window.onload=setTimeout(function(){showTopTen();}, 1000);
+} else{
+	window.onload=setTimeout(function(){showActualLeague();}, 1000);
+}
+
+function showTopTen(){
+	var responseJSON = askServerActualLeague();
+	$("#topLeague").html(parseLeagueTop(responseJSON));
+	appendLeagueMatches(responseJSON);
+}
+
 function showActualLeague(){
-	askServerActualLeague();
+	var responseJSON = askServerActualLeague();
+	$("#topLeague").html(parseLeagueTop(responseJSON));
+	appendLeagueMatches(responseJSON);
 }
+
+
 function askServerActualLeague(){
 	var xmlhttp;
 		if (window.XMLHttpRequest)
